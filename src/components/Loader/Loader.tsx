import { Spinner } from 'tamagui';

import { Container } from '@market-pulse-app/components/Loader/Loader.style';

const Loader = () => (
    <Container>
        <Spinner size="large" />
    </Container>
);

export default Loader;
